---
title: "introduction-to-the-usage-of-the-model-rectangle-frame"
description: "Now when you create a new model in the editor, the system will automatically add a rectangular frame, which is the model frame."
---

Now when you create a new model in the editor, the system will automatically add a rectangular frame, which is the model frame.